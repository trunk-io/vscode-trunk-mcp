# Set Up CI Uploads

The `setup-trunk-uploads` tool helps you configure your CI pipeline to upload test results to Trunk.

Once uploads are configured, Trunk continuously monitors your test suite for flaky tests — no manual tagging required.

Supports GitHub Actions, CircleCI, Jenkins, and other CI providers.
